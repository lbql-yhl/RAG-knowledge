# 产品入门指南（示例）

## 产品定位

这是一个基于检索增强生成（RAG）的 Markdown 知识库示例项目。

## 快速开始

1. 安装项目依赖。
2. 复制 `.env.example` 为 `.env` 并填写模型配置。
3. 将 Markdown 文档放入 `docs/`。
4. 启动服务并在网页中提问。

## 常见问题

### 如何更新知识库？

新增或修改文档后，点击网页右上角的“重建索引”。
