# RAG 知识库问答规范

## 检索链路

系统先按用户已审批的知识库做权限隔离，再分别执行向量检索和 BM25 关键词检索，通过 RRF 融合候选结果，最后使用 Cross-Encoder 重排并取前 3 个片段。

## 回答规则

- 只能根据检索到的片段回答。
- 关键结论使用 `[1]`、`[2]` 引用对应片段。
- 没有足够依据时必须拒答，不能用常识补全。
- 用户没有对应库权限时，不得通过问答间接泄露内容。

## 文档写作建议

标题要表达主题，正文使用短段落和列表；制度、时间、金额、版本号等事实应明确写出，避免只写“及时”“尽快”等模糊词。
