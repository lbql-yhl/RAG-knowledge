# API 接口与代码规范

## 接口约定

- JSON 字段使用 snake_case，错误响应统一返回 `detail`。
- 认证失败返回 401，权限不足返回 403，资源不存在返回 404。
- 写操作应具备幂等性或提供明确的冲突提示。

## Python 规范

函数保持单一职责，外部输入必须校验；数据库查询使用参数绑定；涉及权限的路径必须在服务端再次校验，不能只依赖前端隐藏按钮。

## 提交前检查

运行语法检查、单元测试和最小化接口回归，确认迁移脚本在已有数据库上可重复执行。
