# Design Context — x公司知识库

## Design Context

### Users

公司内部员工（非技术岗为主）。使用情境是**工作间隙的信息查询**：想确认一条制度、找一个报销口径、查一份流程文档。带着具体问题来，希望快点拿到有出处的答案，然后回去干活。

他们不追求"探索感"，追求**可靠感和效率**。界面如果显得花哨或玩具化，会降低他们对答案本身的信任。同时他们会在手机上临时查一下，所以窄屏必须同样好用。

核心任务：
1. 用自然语言问一个问题，拿到基于文档的答案 + 可点开的出处
2. 浏览/检索文档库，打开某篇文档阅读
3. 管理员：审批权限、管理额度、维护文档

### Brand Personality

**沉静 · 可信 · 有温度**

不是"科技感"，是"图书室管理员"的气质 —— 知道东西放在哪，说话有分寸，不会夸大。情绪目标是让用户感觉**这些资料被好好整理过**，所以答案值得相信。

语气：平实、具体、不客套。不用感叹号，不用营销词，不说"赋能""智能助手"这类空话。

### Aesthetic Direction（v5 · 2026-09-23 定稿）

**极简白（Minimal White）** —— 参考 remio 的文档式知识管理界面，经用户从四个候选方案中选定。

- **底色**：纯白 `#FFFFFF`，次级面 `#FAFAFA`。不用任何彩色底。
- **墨色**：主文字 `#1A1A1A`，次级 `#555`，辅助 `#8C8C8C`。层级靠灰阶而非彩色。
- **主色 = 黑**：主按钮、文档选中态（黑底白字）、关键标记统一用墨色，不引入品牌彩色。
- **颜色只表达状态**：绿 `#2FA36B` = 正常/就绪，红 `#D64545` = 危险/错误，琥珀 `#B57717` = 警告。除此之外界面无彩色。
- **分隔**：发丝线 `#EFEFEF / #E4E4E4`（1px）替代阴影与卡片。
- **形状**：圆角 6 / 10 / 14px，输入区胶囊化；无投影或仅一级极浅投影。
- **对话形态**：文档式问答流 —— 无气泡，用户问题加粗、AI 回答为正文，回车间 1px 发丝线分隔，像一份连续沉淀的文档而非聊天记录。

**反参考（明确不要）**：
- ❌ 深色 IDE 风 / 霓虹发光 / 紫蓝渐变 —— 典型的 AI 生成审美
- ❌ 通用 SaaS 后台模板：深色侧栏 + 蓝紫主色 + 满屏卡片 + 药丸标签
- ❌ 聊天气泡、头像堆叠的 IM 式对话
- ❌ 毛玻璃、多层投影、彩色渐变按钮
- ❌ 大面积品牌彩色铺底
- ❌ 每一个动作都加动画

**参考气质**：remio、Linear、Notion 的克制感 —— 内容即界面。

### Design Principles

1. **白纸黑字优先，颜色是稀缺资源。** 页面主体由白底 + 墨色字 + 发丝线构成。黑色只给主行动与选中态，彩色只给状态。60-30-10 里的 10 是真的 10%。

2. **用留白和细线分组，不用卡片。** 需要分组就用间距层级 + 一条细线。只有输入区、弹窗这类真正需要独立交互边界的地方才允许有面。

3. **层级靠 4 个维度同时发力。** 一个标题必须同时是：更大 + 更重 + 颜色更深 + 上方留白更多。绝不只靠字号。

4. **动效只做状态反馈，不做装饰。** 只动 `transform` / `opacity`，`cubic-bezier(.25,1,.5,1)`，100-300ms。`prefers-reduced-motion` 下全部退化。

5. **每个词都要为理解服务。** 按钮写动作（"保存文档"而非"确定"），空态写下一步（而非"暂无数据"），报错写原因 + 怎么办（而非"操作失败"）。不用感叹号，不用表情符号。

### 后端与反幻觉路线（2026-09-23 定）

- **幻觉治理靠显式校验工作流，不靠平台默认功能**：在现有 LangGraph 线性管道（authorize→retrieve→ground→generate→persist）上扩展 grade（检索相关性过滤，CRAG）+ rewrite（查询改写重试 ≤1）+ verify（引用合法性确定性校验 + LLM 事实裁判，Self-RAG）+ revise（带问题清单重生成 ≤1），失败则诚实拒答，结果附 verification 元数据。
- **先在 Dify 搭原型对照**（用户拍板）：Dify 工作流复刻上述节点，与本地管道跑同一组回归问题集，对照幻觉率与拒答率，验证后再回写 `app/rag.py`。
- **算力分工**：Ollama 本地模型做 grade/verify 高频裁判节点（零成本），DeepSeek API 只做最终生成。
- 搭建指南：`DIFY-WORKFLOW-GUIDE.md`（项目根目录；勿放入 docs/ —— 那是知识库语料目录）。

### 系统约束

- **技术栈固定**：FastAPI + 原生 HTML/CSS/JS，无构建步骤。CSS 手写，不引入框架。
- **`data/knowledge.db` 与后端接口不可改动**，`app.js` 仅在必要时做兼容微调。
- **无障碍基线 WCAG 2.2 AA**：正文对比度 ≥ 4.5:1，UI 组件 ≥ 3:1，触控目标 ≥ 44px，全部功能可键盘操作，焦点可见。
- **移动优先**：基线 320px，用 `min-width` 断点向上增强。
- **断点**：480 / 640 / 768 / 1024 / 1280
